# Quasar App

pré requisitos
> install quasar-cli, 
> install vue-cli

instalar dependencias
> yarn install

run
> quasar-dev -m pwa

build
> quasar build -m pwa

Testar build gerado, entre na pasta dist
> quasar serve

